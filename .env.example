# Copy to .env (git-ignored). Only needed if your compose.override.yaml mounts the
# media shares as docker-managed CIFS volumes (see compose.override.example.yaml);
# plain bind mounts need no credentials.
SMB_USERNAME=
SMB_PASSWORD=
