node_modules/
dist/
data/
web/node_modules/
web/dist/

# TypeScript build-mode cache. tsc -b writes one next to the config it builds,
# and neither root config sets tsBuildInfoFile, so it lands at the repo root.
*.tsbuildinfo
.env
compose.override.yaml

# local design tooling workspace and outputs; not needed to build or run Warrden.
.design-sync/
.ds-sync/
ds-bundle/
web/ds-dist/
web/ds-tokens/
.claude/
.superpowers/
